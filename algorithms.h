#ifndef algorithms
#define algorithms

#include<stdlib.h>
#include<stdio.h>
#include<string.h>

void insertion(char** a, int l, int r);
void quicksort(char** a, int l, int r);
int partition(char** a, int l, int r);

#endif